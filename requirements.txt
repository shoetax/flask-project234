Flask==3.0.2
openpyxl==3.1.2
email-validator==2.1.1
Werkzeug==3.0.1
python-dotenv==1.0.0
blinker==1.7.0  # Required for Flask signals (used internally)
itsdangerous==2.1.2  # Security dependency for Flask
Jinja2==3.1.3  # Template engine (bundled with Flask but pinned)
MarkupSafe==2.1.5  # Jinja2 dependency
gunicorn==21.2.0
